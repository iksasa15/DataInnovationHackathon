# ============================================
# الحارس الدلالي — مكتبات بايثون (Python)
# ============================================
# التثبيت: pip install -r requirements.txt
# أو مع بيئة افتراضية: python -m venv .venv && .venv/bin/activate ثم pip install -r requirements.txt
# ============================================

# --- إطار الويب والـ API ---
fastapi==0.115.0
uvicorn[standard]==0.32.0

# --- الذكاء الاصطناعي (التحليل الذكي) ---
google-generativeai==0.8.3
openai==1.54.0

# --- إعدادات ومتغيرات البيئة ---
python-dotenv==1.0.1

# --- التحقق من البيانات والنماذج ---
pydantic==2.9.0

# --- قراءة LFS_Business_Rules.xlsx (قواعد الأعمال الرسمية) ---
openpyxl==3.1.5

# --- طلبات HTTP (للـ API الخارجية) ---
httpx==0.27.2

# pandas / openpyxl: سكربتات محلية فقط — انظر requirements-scripts.txt (مُستبعد من Render لتجنب بناء ضخم وOOM)
